# ── Python 3.11 ──────────────────────────────────────────────────────────────
# Install in a virtualenv: python -m venv .venv && source .venv/bin/activate
# Full install: pip install -r requirements.txt
#
# Airflow has a complex resolver — install it in a dedicated venv or use the
# official constraints file:
#   pip install apache-airflow==2.9.2 \
#       --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.9.2/constraints-3.11.txt"

# ── Core utilities ────────────────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.7.4
pydantic-settings==2.3.4

# ── Data ingestion ────────────────────────────────────────────────────────────
yfinance==0.2.40
pandas==2.2.2
numpy==1.26.4

# ── Airflow (see note above) ──────────────────────────────────────────────────
apache-airflow==2.9.2
apache-airflow-providers-amazon==8.24.0

# ── AWS ───────────────────────────────────────────────────────────────────────
boto3==1.34.131
botocore==1.34.131

# ── Google Cloud / BigQuery ───────────────────────────────────────────────────
google-cloud-bigquery==3.25.0
google-cloud-bigquery-storage==2.25.0
db-dtypes==1.3.0

# ── dbt ───────────────────────────────────────────────────────────────────────
dbt-core==1.8.3
dbt-bigquery==1.8.2

# ── Embeddings & RAG ──────────────────────────────────────────────────────────
langchain==0.2.16
langchain-openai==0.1.23
langchain-community==0.2.16
langchain-core==0.2.40
openai==1.40.0
tiktoken==0.7.0
chromadb==0.5.3

# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.3
httpx==0.27.2

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit==1.36.0

# ── Testing & quality ─────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
ruff==0.5.1
mypy==1.10.1
types-requests==2.32.0.20240622
